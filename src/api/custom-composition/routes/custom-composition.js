'use strict';

/**
 * custom-composition router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::custom-composition.custom-composition');
